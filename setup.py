from distutils.core import setup

setup(
    name = 'invoke-nested-config-example',
    install_requires=[
        'invoke>=0.20,<2.0'
    ],
    author = 'Andrew Aikman',
    author_email = 'aiky30@yahoo.com',
    description = 'An example of an invoke nested config'
)
